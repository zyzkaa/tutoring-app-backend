package com.example.projekt.repository;

import com.example.projekt.model.SchoolDict;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SchoolDictRepository extends JpaRepository<SchoolDict, Integer> {
}
